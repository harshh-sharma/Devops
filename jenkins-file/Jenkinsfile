pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello from Jenkinsfile new one --'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
