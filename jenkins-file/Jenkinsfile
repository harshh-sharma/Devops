pipeline {
    agent {
        label 'agent'
    }

    stages {
        stage('Agent Test') {
            steps {
                sh 'hostname'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}
