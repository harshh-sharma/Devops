pipeline {
    agent {
        label 'linux'
    }

    stages {
        stage('Agent Test') {
            steps {
                sh 'hostname'
                sh 'whoami'
                sh 'pwd'
            }
        }
    }
}
